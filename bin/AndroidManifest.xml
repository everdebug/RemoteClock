<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.txy.remoteclock"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="18" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.txy.remoteclock.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.txy.remoteclock_setting.SettingMain"
            android:label="@string/app_setting" >
        </activity>
        <activity
            android:name="com.txy.remoteclock_setting.NumberSetting"
            android:label="@string/app_setting_numberorcode" >
        </activity>
        <activity
            android:name="com.txy.remoteclock_setting.TextSetting"
            android:label="@string/app_setting_text" >
        </activity>
    </application>

</manifest>